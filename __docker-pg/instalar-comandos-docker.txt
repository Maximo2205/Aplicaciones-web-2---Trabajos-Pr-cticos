/// Crear la imagen
docker build -t imagen_pg .

/// Instanciar un contenedor a partir de la imagen
docker run --name contenedor_pg -e POSTGRES_USER=root -e POSTGRES_PASSWORD=pass -e POSTGRES_DB=chkCompatibility -p 5432:5432 -d imagen_pg

/// Verificar la cracion de la Base de Datos y la tabla

////// Entrar al contenedor
docker exec -it contenedor_pg bash

////// Dentro del contenedor ejecutar:
psql -U root -d chkCompatibility --password
////// Indicar la clave
Password: 

////// Ejecutar consulta
chkCompatibility=# SELECT * FROM componentes;